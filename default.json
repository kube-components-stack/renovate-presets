{
  "regexManagers": [
    {
      "fileMatch": ["^helm-generator\\.yaml$"],
      "matchStrings": [
        "repo: (?<registryUrls>.*?)\\n",
        "name: (?<depName>.*?)\\n",
        "version: (?<currentValue>.*?)\\n"
      ],
      "datasourceTemplate": "helm"
    }
  ]
}